import React from 'react';
import '../../../src/css/TopicPage.module.css';  
import ProblemList from '../../../src/components/ProblemList';  
import SampleProblem from '../../../src/components/SampleProblem';  
import ResourceList from '../../../src/components/ResourceList';
import TopicProgress from '../../../src/components/TopicProgress';  
import Sidebar from '../../../src/components/Sidebar';  
import SignInButton from '@site/src/components/SignInButton';
import remarkMath from 'remark-math';
import rehypeKatex from 'rehype-katex';
import 'katex/dist/katex.min.css';
import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter';
import { okaidia } from 'react-syntax-highlighter/dist/esm/styles/prism';


export const moduleId = "LearnToCode"; 
export const topicId = "BasicDataTypes";  

<Sidebar moduleId={moduleId} /> 

# Basic Data Types

Prerequisites: Nothing

# Resources

<ResourceList moduleId={moduleId} topicId={topicId}/>

So you just used a string but there are really $3$ import data types you should know for now: 

<div style={{ transform: 'translateX(150px)' }}>
  <SyntaxHighlighter language="cpp" style={okaidia}>
{`string a; //words
int b; //numbers less than a couple billion (10^9)
long long c; //numners less than a couple quintillion (10^18)
`}
  </SyntaxHighlighter>
</div>

Here is more syntax (or way that the code for certain logic is written): 

<div style={{ transform: 'translateX(150px)' }}>
  <SyntaxHighlighter language="cpp" style={okaidia}>
{`int a = 0; cout << a << endl;
    a = 3; cout << a << endl;
    a = 4 + 7 - 2; cout << a << endl; //addition and subtraction
    a = a + 1; cout << a << endl; //can use variables in equaitons
    a = a * 3; cout << a << endl; //multiplication
    a = 5 / 3; cout << a << endl; //division rounds down to nearest integer number
    a = 5 % 3; cout << a << endl; //modular funciton that computes remainder; optional  
`}
  </SyntaxHighlighter>
</div>

Note: The code above would also work if a was a long long. 

<div style={{ transform: 'translateX(150px)' }}>
  <SyntaxHighlighter language="cpp" style={okaidia}>
{`string a = "cat"; cout << a << endl; 
a = "dog"; cout << a << endl; 
`}
  </SyntaxHighlighter>
</div>

# Focus Problem

<SampleProblem  moduleId={moduleId} topicId={topicId} location={"sample1"} /> 

<TopicProgress moduleId={moduleId} topicId={topicId} />



